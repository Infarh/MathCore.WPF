using System;
using System.Collections.Generic;
using System.IO;
using System.Linq;

namespace MathCore.WPF.ConsoleTest
{
    class Program
    {
        static void Main(string[] args)
        {
            //var lines = GetLines("TestFile.txt").Take(5);
        }

        //private static IEnumerable<string?> GetLines(string FileName)
        //{
        //    using var reader = new StreamReader(FileName);
        //    while (!reader.EndOfStream)
        //        yield return reader.ReadLine();
        //}
    }

    //class TestActionClass
    //{
    //    private Action<int> _Action;

    //    public TestActionClass(Action<int> action) => _Action = action;

    //    public TestActionClass(bool IsNullSelector, Action action)
    //        :this(IsNullSelector ? (Action<int>)null : x => action())
    //    {
            
    //    }
    //}
}
